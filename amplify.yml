version: 1
applications:
  - appRoot: maintenance
    frontend:
      phases:
        preBuild:
          commands:
            - echo "Starting preBuild phase"
        build:
          commands:
            - echo "Starting build phase"
      artifacts:
        baseDirectory: .
        files:
          - index.html
      cache:
        paths: []
