version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting preBuild phase"
    build:
      commands:
        - echo "Starting build phase"
  artifacts:
    baseDirectory: maintenance
    files:
      - index.html
  cache:
    paths: []
