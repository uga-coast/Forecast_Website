version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "Installing dependencies"
            - npm ci
        build:
          commands:
            - echo "No build necessary for maintenance page"
      artifacts:
        baseDirectory: maintenance
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: .
